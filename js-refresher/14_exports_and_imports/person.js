// an object usually uses const to make
// object properties no need semicolon
const person = {
    name : 'Max'
}

// this means that by default, it will auto export the person object
export default person